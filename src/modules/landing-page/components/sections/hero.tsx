import { Container } from '@/global/components/container';
import { ContentPadding } from '@/global/components/content-padding';

export const HeroSection = () => {
	return (
		<Container className="py-10">
			<ContentPadding>Hero</ContentPadding>
		</Container>
	);
};
